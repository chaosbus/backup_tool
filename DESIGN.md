# 跨平台应用备份工具 — 功能方案设计

> 状态：定稿。目标：Windows / Linux 上按应用打包备份配置目录，提供 GUI（Tauri）、TUI（ratatui）、CLI（headless）三种前端，共享同一核心引擎。

---

## 一、关键决策

| # | 问题 | 决策 | 理由 |
|---|------|------|------|
| Q1 | 应用选择 | 配置文件为主 + GUI 可增删改 | 配置是事实来源，GUI 编辑回写同一文件；勾选只是运行时行为 |
| Q2 | 配置格式/位置 | JSON + 可自定义路径 | 默认 `~/.backup_tool/config.json`（Win: `%APPDATA%\backup_tool\config.json`），支持 `--config <path>` 和 GUI 内改路径；GUI 内可直接编辑保存 |
| Q3 | 备份存储 | 用户指定基目录 + 每次生成时间戳 | 格式可选项：`zip`（默认）/ `tar.gz` / 纯目录复制 |
| Q4 | 备份范围 | 仅用户指定路径 + 支持排除规则 | 不做自动发现；排除用 glob，支持全局排除 + 每应用排除 |
| Q5 | 恢复功能 | 需要（P2） | 设计时预留恢复数据模型 |
| Q6 | 定时备份 | 先不做内置定时器，但提供无头 CLI | v1 提供 `backup-tool backup --all`，用户用 cron/Task Scheduler 调度 |
| Q7 | GUI | 功能型 | 应用列表 + 备份历史 + 日志 + 配置管理；必须有进度条和日志 |
| Q8 | 技术选型 | Tauri v2（Rust 核心 + Web 前端） | 单文件小体积、跨平台 fs 操作稳、zip/tar 有成熟 crate；Rust 核心可复用出 TUI |
| Q9 | 分发 | 单个可执行文件 | Windows 出 NSIS 安装包，Linux 出 AppImage/deb |

---

## 二、总体架构

分层设计，三个前端共享同一核心：

```
┌───────────────────────────────────────────────┐
│  前端层                                        │
│  GUI (Tauri v2, 内嵌 Web UI)    TUI (ratatui) │
├───────────────────────────────────────────────┤
│  核心引擎 (Rust crate: backup-core)           │
│  配置加载 │ 路径解析 │ 打包 │ 恢复 │ 历史 │ 日志 │
├───────────────────────────────────────────────┤
│  CLI 层 (headless)                            │
│  backup-tool backup --all --config <path>     │
└───────────────────────────────────────────────┘
```

- **核心引擎**为纯 Rust 库，无 UI 依赖，三种前端都调它。
- 核心通过事件回调上报进度（`Event::Progress{percent}`、`Event::FileDone{path}`、`Event::Log{level,msg}`）。
- GUI 和 TUI 都只是核心的薄封装，行为一致。

---

## 三、配置文件规范（版本化 JSON）

```json
{
  "version": 1,
  "backup": {
    "dest": "~/backups",
    "format": "zip",
    "parallel": 2,
    "retention": 10,
    "cleanup": "after_each",
    "checksum": true,
    "excludes": ["**/.DS_Store", "**/desktop.ini"]
  },
  "apps": [
    {
      "id": "vscode",
      "name": "VSCode",
      "enabled": true,
      "paths": {
        "windows": ["%APPDATA%\\Code\\User"],
        "linux": ["$HOME/.config/Code/User"],
        "macos": ["$HOME/Library/Application Support/Code/User"]
      },
      "excludes": ["Cache/**", "*.log"],
      "compress": true
    }
  ]
}
```

要点：
- **`paths` 按 OS 分开定义**，运行当前系统时只解析对应键，天然跨平台。
- 支持变量展开：`%APPDATA%`、`%USERPROFILE%`（Win），`$HOME`、`~`（Linux），引擎内置展开表。
- `apps` 数组而非对象，保留排序；`id` 用于命名备份文件和去重。
- **配置校验**：启动时校验 schema、路径存在性（缺失标黄警告但允许备份），损坏时报错并给出样例。
- 允许运行时不改文件、仅内存覆盖（GUI 临时勾选）。

---

## 四、备份存储规范

```
{backup_dest}/
├── history.json                 # 索引（GUI 快速读取，可重建）
├── vscode/
│   ├── vscode_20260822_093000.zip
│   ├── vscode_20260822_093000.zip.summary.json
│   ├── vscode_20260821_180000.zip
│   └── vscode_20260821_180000.zip.summary.json
└── chrome/
    ├── chrome_20260822_093000.zip
    └── chrome_20260822_093000.zip.summary.json
```

- **按应用分子目录**，文件命名 `{app_id}_{YYYYMMDD_HHmmss}.{ext}`。
- `history.json` 记录备份元数据；GUI 优先读它，若文件丢失则扫描目录重建（自愈）。
- **`retention`**：备份后删除超出 N 份的旧备份（0 = 全保留），删前写日志；删除归档时**同步删除**同名 `.summary.json` sidecar。
- **原子性**：打包先写 `*.zip.tmp`，完成后重命名，避免崩溃产生半个文件。
- **大文件处理**：zip 用流式写入；单文件超阈值（如 2GB）提示可跳过。
- **归档目录层级**：归档内保留源路径根目录的 basename 层级，例如配置 `%APPDATA%\example\` 时归档内为 `example/...`，恢复时据此还原目录结构。

### 4.1 备份 summary（sidecar）

每次成功备份都会在归档同级生成 `<归档文件名>.summary.json`，作为该次备份的机器可读元数据（也用于 `history.json` 丢失时的重建）：

```json
{
  "version": 1,
  "app_id": "vscode",
  "app_name": "VSCode",
  "platform": "windows",
  "archive": "vscode_20260822_093000.zip",
  "format": "zip",
  "size": 50331648,
  "files": 214,
  "bytes_total": 50331648,
  "started_at": "2026-08-22T09:30:00Z",
  "finished_at": "2026-08-22T09:30:12Z",
  "checksum": { "algorithm": "sha256", "digest": "a1b2c3d4…" },
  "status": "ok",
  "excludes": ["Cache/**", "*.log"],
  "paths": [
    {
      "raw": "%APPDATA%\\example",
      "resolved": "C:\\Users\\joe\\AppData\\Roaming\\example",
      "existed": true,
      "archive_root": "example",
      "restore_to": "C:\\Users\\joe\\AppData\\Roaming\\example"
    }
  ]
}
```

字段说明：

- `version`：summary 格式版本号；`app_id` / `app_name`：备份时的应用标识与显示名；`platform`：备份所在平台。
- `archive`：归档文件名；`format`：`zip` | `tar.gz` | `dir`；`size` / `files` / `bytes_total`：本次备份总量信息。
- `started_at` / `finished_at`：备份机器本地时间的 RFC3339 字符串（含时区偏移）；`checksum`：`zip`/`tar.gz` 为 `{ "algorithm": "sha256", "digest": "..." }` 对象，`dir` 为 `null`；`status`：备份结果状态。
- `excludes`：本次实际生效的排除规则（全局 + 应用合并后的结果）。
- `paths`：每个参与备份的源路径一项，字段含义：
  - `raw`：配置中的原始路径字符串；
  - `resolved`：变量展开并规范化后的绝对路径；
  - `existed`：备份时该路径是否存在（缺失项不参与打包）；
  - `archive_root`：该路径在归档内的根目录名（对应归档 basename 层级）；
  - `restore_to`：手工恢复目标，即原始 `resolved` 路径——满足"哪个目录恢复到哪"，恢复时按此值还原到原位置，不依赖用户记忆。

**与 retention / rebuild 兼容**：`retention` 按份数删除旧归档时同步删除对应 `.summary.json`；`history.json` 缺失重建时，优先读取归档旁同名的 `.summary.json` 恢复条目字段。

---

## 五、核心引擎：路径解析与环境变量展开

### 5.1 解析管线

```
原始字符串 → ① 平台语法识别 → ② 变量展开 → ③ 规范化(绝对化/去尾斜杠)
→ ④ 存在性检查 → ⑤ 状态分类(有效/跳过/错误/其他平台) → ⑥ 进入备份或报错
```

### 5.2 识别与展开规则

| 平台 | 匹配模式 | 展开来源 | 未定义时 |
|------|---------|---------|---------|
| Windows | `%NAME%`（`%` 包裹，不区分大小写） | `os::env` 查 `NAME` | 状态=错误 |
| Windows | `~` 开头 | 不支持 → 提示改用 `%USERPROFILE%` | 状态=错误 |
| Linux | `$NAME`、`${NAME}` | `os::env` 查 `NAME` | 状态=错误 |
| Linux | `~/` | 等价 `$HOME/` | `$HOME` 缺失→错误（罕见） |
| 跨平台（通用） | 相对路径（无变量无 `~/`） | 相对**配置文件所在目录**解析 | — |

- **嵌套展开**：递归展开直到无变量为止，最多 5 层防死循环。
- **转义**：不做转义字符；`%`/`$` 出现的明文路径视为"疑似变量"，校验时提示。
- **平台语法错配**（当前平台是 A 却写了 B 语法）→ 展开为"未定义变量"，标记 `other_platform`，**不报错**。

### 5.3 状态分类（每个 `app.path` 的最终结果）

| 状态 | 条件 | 备份时行为 |
|------|------|-----------|
| `valid` | 展开成功且路径存在 | 参与打包 |
| `valid_missing` | 展开成功但路径不存在 | **跳过 + warn 日志** |
| `error_undefined` | 变量未定义（当前平台真缺失） | **跳过 + warn**，D3 中列红 |
| `error_permission` | 存在但无读权限 | **跳过 + warn** |
| `other_platform` | 语法错配，只影响他平台 | 跳过，静默（仅 debug 日志） |

> 统一原则：**单路径失败 = 跳过并记录，不中断整次备份**；一个应用内**全部路径都失败**时才把该应用记为"失败"，其余照常完成。

### 5.4 边界情况

| 场景 | 处理 |
|------|------|
| 空路径 / 纯空白 | 保存时阻止，实时校验提示 |
| 重复路径（同应用内） | 去重，保留首个 |
| 同一目录被两个应用引用 | 允许，各自独立打包 |
| 路径含空格/中文/`%`字母 | 变量正则按 `%NAME%` 严格匹配，其余原文保留；全程 UTF-8 |
| 尾部 `\` `/` | 规范化时去掉 |
| `.` `..` 片段 | 规范化时折叠 |
| 变量指向文件而非目录 | 允许（备份单文件）；存在性按文件判 |
| 符号链接 | 打包时保留链接本身（不递归展开目标） |
| Windows 变量值含 `\` | 原样使用，不转义 |
| 路径超长（>260 字符） | 系统开启长路径则放行，否则 warn |

### 5.5 校验时机

1. **实时**（应用编辑页）：输入即展开 + 存在性预览。
2. **启动**（主界面加载前）：全量校验 → D3 警告清单。
3. **备份执行时**：最终把关，状态分类决定 skip/继续。

---

## 六、核心引擎：备份并行与进度

### 6.1 并行模型

```
配置: parallel: N (默认 2, 范围 1..=应用数)
┌──────────────┐
│ 任务队列      │  apps: [vscode, chrome, ssh, git, docker, proj]
└──────┬───────┘
       │ 取一个应用
┌──────▼───────┐  每个 worker 独占一个压缩流 + 1MB 缓冲
│ Worker 池     │
│  w1: app      │  应用内部: 路径逐个处理, 文件流式读写(单线程)
│  w2: app      │  各 worker 独立写自己的 .tmp
└──────┬───────┘
       │ 完成即清理 → 取下一个
```

- **并发粒度 = 应用**，不做同应用内文件并行。
- 取任务按配置顺序，**完成顺序不保证**。
- SSD → `2~4`；HDD → `1`（多写头争抢反慢）；`parallel × 内存缓冲(≈1MB) ≤ 预期可用内存`。
- 同一目标目录多写 `.tmp`，命名含 worker 序号防冲突；原子改名收尾。

### 6.2 进度模型（两维 + 两阶段）

**两维**：文件数 + 字节数。进度条百分比以**字节**为主，文件计数为辅（`文件 12/15`）。

**每应用两阶段**：
```
Phase A  扫描/枚举: 遍历统计 {files_total, bytes_total}
         → 不定态(转圈 "扫描中… 12,400 文件")
Phase B  打包/写入: 流式压缩, 每文件完成后上报增量
         → 确定进度条(百分比 + 文件计数)
```

**整体进度（跨应用）**：`overall = 已完成字节和 / 已扫描应用的字节目标和`；未开始扫描的应用不贡献分母（总量未知），随新应用进入会跳变。

### 6.3 事件流（核心 → UI）

| 事件 | 负载 | 频率 |
|------|------|------|
| `AppStarted` | `{app_id}` | 应用开始 |
| `ScanUpdate` | `{app_id, files_scanned}` | 扫描期节流 |
| `ScanDone` | `{app_id, files_total, bytes_total}` | Phase A 完成 |
| `FileDone` | `{app_id, path, bytes_written}` | 每文件 |
| `AppProgress` | `{app_id, files_done, files_total, bytes_done, bytes_total, eta}` | 每文件聚合 |
| `OverallProgress` | `{apps_done, apps_total, bytes_done, bytes_total, eta}` | 聚合推送 |
| `AppFinished` | `{app_id, result: ok\|skip\|error, detail, size, checksum}` | 应用结束 |
| `Log` | `{level, msg}` | 任意 |

### 6.4 界面呈现（操作/日志区）

```
┌ 操作 / 日志 ─────────────────────────────────────────┐
│ [备份所选] [停止]          并行: 2                  │
│ 总进度  ████████████░░░░░░  63%  48/72 MB   ETA 0:12 │
│ ─────────────────────────────────                    │
│ ☑ VSCode   ██████████████  100%  48 MB  ✓ 12s       │
│ ☑ Chrome   ════════╪══  扫描中…  412,000 文件   ⏳   │
│ ☐ SSH      等待中                                     │
│ ─────────────────────────────────                    │
│ 12:01:03 ✓ vscode 完成 48MB                          │
│ 12:01:04 文件 ✓ User/keybindings.json                │
└──────────────────────────────────────────────────────┘
```

### 6.5 节流与批量

- 核心事件写入**有界 channel**；UI 消费端**每 100ms 合并一帧**再渲染。
- `ScanUpdate` 直接 100ms 节流；进度事件由 UI 帧驱动聚合。
- 核心侧不等待 UI——channel 满则丢最旧进度事件，保留关键事件。

### 6.6 取消

- [停止] → 取消标志，checkpoint 在**文件边界**；当前文件写完即停。
- 清理各自 `.tmp`；已完成的 app 保留并进 history，未完成标记 `cancelled`。

### 6.7 完成汇总与 retention

```
全部 worker 空闲 → D2 完成报告(成功/失败/跳过/取消)
→ 对成功 app 触发 retention 清理(保留 N 份)
```
- `cleanup: "after_each"`（默认，尽早释放磁盘）| `"at_end"`。

### 6.8 边界情况

| 场景 | 处理 |
|------|------|
| 扫描期应用崩溃/取消 | 丢弃该 app，其余继续 |
| 同应用含超大单文件 | 进度按字节平稳；单文件阈值(默认 2GB)可跳过 |
| 扫描总量为 0（空目录） | 仍打包，进度瞬间 100% |
| ETA 计算 | 基于滚动窗口速率（最近 30s），首 5s 显示 `估算中…` |
| 并行写同一磁盘 IO 瓶颈 | 日志 warn 建议降低并行 |
| 事件风暴（万级小文件） | 见 6.5 节流；内存由有界 channel 保护 |

---

## 七、核心引擎：恢复冲突策略

### 7.1 冲突判定模型（恢复前先"预扫描"生成计划）

| 判定 | 条件 | 分类 |
|------|------|------|
| `new` | 目标不存在 | 无冲突，直接写入 |
| `identical` | 目标存在且内容相同 | 无冲突，**跳过**（计入"已是最新"） |
| `different` | 目标存在且内容不同 | **冲突** |
| `type_mismatch` | 类型不一致（备份是文件/目标是目录，或反之） | **冲突**（需专门策略） |
| `symlink` | 备份为链接 / 目标是链接 | **冲突**（单独处理） |
| `locked` | 目标被占用/无写权限（Windows 常见） | **错误**（记录，跳过） |

> 内容比较：先比**大小**（秒过），同大小再**流式逐字节比对**。比对只在"将覆盖的文件"上做。

### 7.2 策略定义（全局设置，可对单文件覆盖）

| 策略 | `different` | `type_mismatch` | `symlink` | 说明 |
|------|------------|-----------------|-----------|------|
| **覆盖 overwrite**（默认） | 用备份版替换 | 按替换规则处理 | 替换 | 最常用 |
| **跳过 skip** | 保留目标，丢弃备份版 | 保留目标 | 保留目标 | 计入"跳过 N" |
| **保留两者 keep_both** | 备份版写为 `name.conflict-{ts}.ext` | 同名加后缀 | 后缀化 | 最安全，零丢失 |
| **保留较新 keep_newer**（可选） | 按 mtime 保留较新者 | 等价 keep_both | — | 防覆盖掉新改动 |

- `type_mismatch` 的替换规则（覆盖策略下）：**先移走目标**（进安全网）再写入；**不做直接删除**。

### 7.3 执行时序

```
预扫描 → 生成计划 {new, overwrite, skip, conflict, mismatch}
→ 展示计划 → 用户确认 → 执行:

  ① 解压备份到临时目录 (dest/.restore-tmp/{app}-{ts}/)
  ② 移动安全网: "将被覆盖"的目标原文件 → {target}/.pre-restore-{ts}/
  ③ 逐文件: 写 .tmp → 原子改名落入目标
     (keep_both → 改名加后缀; skip → 不写, 仅计数)
  ④ 清理临时目录; 写日志 + 恢复报告
```

- **单文件粒度失败**：不中断整体，记 `locked`/`error`，最后汇总。
- 安全网开关（默认开）在向导步骤 3。

### 7.4 回滚

- `.pre-restore-{ts}` 保留原文件 → **"回滚本次恢复"**：读日志中的安全网路径，把原文件放回、删除本次写入。
- 恢复完成报告出现 `[回滚]`；`.pre-restore-*` 默认保留 7 天自动清理。

### 7.5 UI（向导步骤 3→4）

```
步骤3: 目标与冲突（置灰）
  冲突策略   (●) 覆盖现有文件   ( ) 跳过   ( ) 保留两者   ( ) 保留较新
  覆盖保护   [✓] 覆盖前将原目录备份为 .pre-restore-时间戳
  [查看冲突文件列表(28 个)]  →  展开面板 (每文件可改策略)
步骤4: 确认（置灰）(预扫描后)
  ⚠ 将写入 214 项:  新增 28 · 覆盖 186 · 跳过 0 · 冲突解决后 0 保留
  [回滚保护: 将备份原 186 个文件到 .pre-restore-20260822_0930]
                              [← 上一步] [取消] [执行恢复]
```

- 步骤 4 数字来自步骤 3 选完策略后重跑预扫描的结果。

### 7.6 边界情况

| 场景 | 处理 |
|------|------|
| 目标目录本身就是符号链接 | 视为链接冲突，按 symlink 策略；不跟随写入 |
| 备份含 Windows 非法字符（Linux 恢复） | `:`、`*` 等 → 替换为 `_`，计入日志 |
| 恢复目标 = 备份文件所在目录 | 拦截（防自恢复污染备份库） |
| 目标磁盘空间不足 | 预扫描估算写入量，先检查可用空间，不足阻止并提示 |
| 覆盖过程中断电 | 原子改名保证无半文件；`.pre-restore` 可回滚 |
| 目标有备份中不存在的额外文件 | 保留不动（不做镜像同步） |
| 大小相同但内容不同 | 逐字节比对兜底 |

---

## 八、history.json 索引字段

```json
{
  "version": 1,
  "entries": [
    {
      "app_id": "vscode",
      "file": "vscode_20260822_093000.zip",
      "format": "zip",
      "size": 50331648,
      "files": 214,
      "bytes_total": 50331648,
      "started_at": "2026-08-22T09:30:00Z",
      "finished_at": "2026-08-22T09:30:12Z",
      "checksum": "a1b2…",
      "status": "ok"
    }
  ]
}
```

- GUI 优先读 index；文件丢失时扫描目录重建（自愈）。
- 删除备份时同步更新 index；恢复/回滚在日志中留痕。

---

## 九、页面清单与导航

### 9.1 导航图

```
                    ┌──────────────┐
       启动 ───────▶│ 配置校验(启动) │──⚠ 有问题──▶ D3 路径缺失警告
                    └──────┬───────┘                │忽略继续
                           │ 通过/忽略               ▼
                           │              ┌──────────────────────┐
                           ▼              │       主界面          │
                    ┌──────────────┐      │ 应用列表/历史/操作区   │
                    │  D3 警示      │────▶└──┬───────┬───────┬────┘
                    └──────────────┘        │       │       │
            ┌───────────────────┬───────────┼───────┼───────┼──────────────┐
            │                   │           │       │       │              │
            ▼                   ▼           ▼       ▼       ▼              ▼
      ┌──────────┐      ┌───────────┐  ┌────────┐ ┌──────┐ ┌──────┐  ┌──────────┐
      │ 应用编辑  │◀────│ 新建应用   │  │ D1确认 │ │ D4删 │ │ 设置 │  │ 历史详情  │
      │ (抽屉)    │      └───────────┘  └───┬────┘ └──┴───┘ └──┬───┘  └────┬─────┘
      └────┬─────┘                          │执行备份           │保存      │
           │保存/删除                         ▼                  └──▶主界面   │
           └──────────▶ 主界面(列表刷新)   ┌────────┐                    │
                                          │ 执行中  │ (总进度+每应用)     │
                                          │ 操作/日志│──[停止]──▶取消清理  │
                                          └───┬────┘                    │
                                              ▼                          │
                                          ┌────────┐──[回滚]──▶ 回滚执行  │
                                          │ D2报告  │                    │
                                          └────────┘                    │
                                                                ┌────────▼────────┐
  恢复:  主界面历史行 ─▶ 恢复向导(单对话框 4 步)                  │ 恢复向导        │
         ①选备份 ─▶ ②预览 ─▶ ③目标+冲突策略 ─▶ ④确认 ─▶ 执行      │ ①选备份 ②预览    │
                                                                │ ③目标+策略 ④确认 │
                                                                └─────────────────┘
```

### 9.2 页面与跳转汇总

| 页面/对话框 | 入口 | 出口 | 写配置? |
|-------------|------|------|---------|
| 主界面 | 启动/所有页面返回 | 全部 | — |
| 应用编辑(抽屉) | 主界面 [+添加]/[编辑]/双击行 | 保存/取消/删除 → 主界面刷新 | ✓ 写 config.json |
| 设置 | 主界面[设置] | 保存/取消 → 主界面 | ✓ 写 config.json |
| 历史详情 | 主界面历史栏双击/右键打开 | 返回 / [恢复…]→向导 / 删除→D4 | — |
| D1 备份确认 | 主界面[备份所选]/[全部备份] | [备份]→执行中 / 取消 | 可选(格式覆盖) |
| 执行中(操作区态) | D1 确认 | [停止]→取消 / 完成→D2 | ✓ 写 history+日志 |
| D2 完成报告 | 执行完成 | [打开目录]/[关闭] | — |
| D3 路径缺失警告 | 启动校验 | [继续]/[去修复]→应用编辑 | 可选 |
| D4 删除确认 | 历史详情/历史栏右键 | [删除]/[取消] | ✓ 写 history.json |
| 恢复向导(对话框4步) | 主界面[恢复]/历史详情[恢复…] | 完成→报告+[回滚] | — |

### 9.3 规则与约束

- **单枢纽**：主界面是唯一常驻页，其余全部为**抽屉/对话框**，导航深度恒为 1。
- **写回时机**：应用编辑/设置保存即写 config.json 并即时生效；备份/恢复只写 `history.json` + 日志。
- **无头模式**：CLI 完全绕开 GUI 状态机，只走核心引擎 + 文件日志。
- **异常路径**：备份中应用失败→继续剩余→D2 汇总；恢复中断→安全网回滚；取消→清理 .tmp 保留已完成。

---

## 十、界面原型

### 10.1 主界面

```
┌────────────────────────────────────────────────────────────────────────────┐
│ ☰ 应用备份工具                     [全部备份] [恢复] [设置] [退出]          │
├───────────────────────────────┬──────────────────────┬──────────────────────┤
│  应用列表             (3/6)    │  备份历史 (vscode)   │  操作 / 日志         │
├───────────────────────────────┼──────────────────────┼──────────────────────┤
│ ☑ VSCode     上次 08-22 09:30 │ 08-22 09:30  48 MB   │ [备份所选] [停止]    │
│ ☑ Chrome     上次 08-22 09:30 │ 08-21 18:00  52 MB   │                      │
│ ☐ SSH        从未备份          │ 08-20 10:12  51 MB   │ ██████████░░░ 80%   │
│ ☐ Git        从未备份          │ 08-19 09:01  49 MB   │ 12 / 15 文件         │
│ ☐ Docker     从未备份          │                      │                      │
│ ☐ 自定义项目  从未备份          │                      │ ✓ vscode   完成 48MB │
│ ──────────────────────         │                      │ ✓ chrome   完成 312M │
│ [+ 添加应用] [编辑] [删除]     │                      │ ⚠ ssh: Cache 被占用 │
│                                │                      │ ⏳ git     进行中…   │
├───────────────────────────────┴──────────────────────┴──────────────────────┤
│ 备份位置: /home/user/backups/   格式: zip    保留: 10    上次备份: 08-22 09:30 │
└────────────────────────────────────────────────────────────────────────────┘
```

- 中栏顶部下拉可切换"按应用看历史 / 全部历史"。
- 双击应用行 → 应用编辑页；右键历史条目 → 删除/打开文件夹。

### 10.2 应用编辑（抽屉式；路径仅显示当前平台，环境变量实时解析）

> **ID 自动生成**：ID 由程序在保存时根据名称自动生成 slug（小写字母/数字/`-`），与已有 ID 冲突时自动追加后缀（如 `vscode` → `vscode-2`）；界面不显示 ID，用户不可修改。
> **备份路径**：同时支持手动输入与系统目录选择（[浏览目录] 打开系统选择器）；手动输入时实时解析环境变量并校验存在性。

```
┌───────────────────────────────────────────────────────────────────────────┐
│ ◀ 返回          编辑应用                          [删除]    [保存]         │
├───────────────────────────────────────────────────────────────────────────┤
│  名称*    [ VSCode                                          ]             │
│  启用     [●] 在备份列表中显示                                                 │
│  压缩     [●] 打包为压缩包      ○ 直接复制目录                                  │
│                                                                           │
│  备份路径                                        [系统: Linux ✓]           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  $HOME/.config/Code/User                           [−] [浏览目录]      │ │
│  │      → /home/joe/.config/Code/User          ✓ 存在                  │ │
│  │  %APPDATA%\Code\logs                          [−] [浏览目录]            │ │
│  │      → ⚠ 未定义变量 %APPDATA%（当前为 Linux）                       │ │
│  │      → 该路径仅影响 Windows，可忽略                                 │ │
│  │                                                                     │ │
│  │                                       [+ 添加路径]                  │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  排除规则 (glob)                                                          │
│  [ Cache/**                       ]  [−]                                  │
│  [ *.log                          ]  [−]                                  │
│  [+ 添加]                                                                 │
│                                                                           │
│  ⚠ 未配置路径 → 在备份列表中将显示警告并在备份时跳过该应用                    │
└───────────────────────────────────────────────────────────────────────────┘
```

### 10.3 设置页

> **本期实现范围**：备份位置 / 备份格式 / 并行备份数 / 保留份数 / 清理时机 / 全局排除 / 恢复默认 / 重新加载；配置文件路径仅显示当前文件（不可切换）。日志设置与"关于"区域不在本期范围。

```
┌───────────────────────────────────────────────────────────────┐
│ ☰ 设置                                        [恢复默认] [保存]│
├───────────────────────────────────────────────────────────────┤
│ 备份位置    [ /home/user/backups/                ] [浏览]      │
│ 备份格式    (●) zip   ( ) tar.gz   ( ) 目录复制               │
│ 并行备份数  [ 2  ]   (1 = 串行)                               │
│ 保留份数    [ 10 ]   (0 = 永久保留)                           │
│ 清理时机    (●) 每个应用完成后   ( ) 全部结束后                │
│ 全局排除    [ **/.DS_Store            ] [−]                   │
│             [ **/desktop.ini          ] [−]                   │
│             [ + 添加 ]                                        │
│ 配置文件路径 [ ~/.backup_tool/config.json ]（仅显示当前文件）   │
│             [重新加载]                                        │
│ 日志        (●) 写入文件   ( ) 仅界面   级别: [Info ▾]（非本期）│
├───────────────────────────────────────────────────────────────┤
│ 关于   v0.1.0   核心引擎 0.1.0  检查更新    （非本期）           │
└───────────────────────────────────────────────────────────────┘
```

### 10.4 恢复向导（单对话框 4 步）

> **当前状态：UI 占位**。本期仅实现步骤 1（选择备份，展示历史备份列表）；步骤 2~4 与"执行恢复"按钮置灰。完整恢复（预览、冲突策略、执行、回滚）为 P2 范围。

```
步骤1: 选择备份
  应用   [ VSCode        ▾ ]
  备份   (●) 08-22 09:30  48 MB
         ( ) 08-21 18:00  52 MB
                              [下一步 →]

步骤2: 预览（置灰）
  将恢复 214 个文件, 共 48 MB   目录: 3  文件: 211  链接: 0
  ├─ User/…
  ├─ keybindings.json
  └─ …

步骤3: 目标与冲突（置灰）
  恢复目标   (●) 原位置 $HOME/.config/Code/User
            ( ) 自定义  [ /tmp/restore/vscode ] [浏览]
  冲突策略   (●) 覆盖现有文件  ( ) 跳过  ( ) 保留两者  ( ) 保留较新
  覆盖保护   [✓] 覆盖前将原目录备份为 .pre-restore-时间戳
  [查看冲突文件列表(28 个)] → 每文件可单独改策略

步骤4: 确认（置灰）
  ⚠ 将写入 214 项: 新增 28 · 覆盖 186 · 跳过 0 · 冲突解决后 0 保留
                              [← 上一步] [取消] [执行恢复]
```

### 10.5 历史详情

```
┌─────────────────────────────────────────────────────────────┐
│ ◀ 返回   vscode_20260822_093000.zip      [删除] [打开文件夹] │
├─────────────────────────────────────────────────────────────┤
│ 应用   VSCode            格式  zip                         │
│ 时间   2026-08-22 09:30  大小  48 MB                       │
│ 来源   ~/.config/Code/User         校验和  a1b2…c3d4 ✓     │
│ ─────────────────────────────────────────────────────────── │
│  文件清单                      搜索 [ filter…            ] │
│  ├─ User/… (214 项)                                        │
│  ├─ User/keybindings.json       4 KB                      │
│  ├─ User/settings.json          18 KB                     │
│  └─ …                                                     │
│                                            [恢复…]          │
└─────────────────────────────────────────────────────────────┘
```

### 10.6 对话框

```
D1 备份确认                        D2 完成报告
┌──────────────────────────────┐  ┌──────────────────────────┐
│ 确认备份                      │  │ 备份完成  用时 12s       │
│ ☑ VSCode     48 MB           │  │ ✓ vscode   48 MB        │
│ ☑ Chrome     312 MB          │  │ ✓ chrome   312 MB       │
│ ☐ SSH        0 B(无路径)      │  │ ✗ git      失败: 权限   │
│ 格式   (●) zip  ( ) tar.gz   │  │ 跳过   1  共 3           │
│ 目标   /home/user/backups/   │  │        [打开目录] [关闭]  │
│                 [取消] [备份] │  └──────────────────────────┘
└──────────────────────────────┘
D3 路径缺失警告                  D4 删除确认
┌──────────────────────────────┐  ┌──────────────────────────┐
│ ⚠ 配置检查发现 2 个问题        │  │ 删除备份                 │
│ • SSH: linux 路径不存在        │  │ vscode_20260820_1012.zip │
│ • Git: 未配置当前系统路径      │  │ 48 MB · 2026-08-20       │
│   [继续备份] [忽略并保存]      │  │ 此操作不可撤销            │
│   [去修复(编辑应用)]           │  │        [取消] [删除]      │
└──────────────────────────────┘  └──────────────────────────┘
```

### 10.7 TUI（终端版，同核心）

```
┌──────────────────────────────────────────────┐
│ 应用备份工具 v0.1.0           b:备份 r:恢复   │
├──────────────┬───────────────────────────────┤
│ 应用列表      │ 历史  (↑↓ 移动  Space 勾选)  │
│ ☑ VSCode  48M│ 08-22 09:30 48 MB            │
│ ☑ Chrome 312M│ 08-21 18:00 52 MB            │
│ ☐ SSH       -│ 08-20 10:12 51 MB            │
│              │ 日志:                        │
│              │  ✓ vscode 完成 48MB          │
│              │  ⚠ ssh: Cache 被占用         │
├──────────────┴───────────────────────────────┤
│ 位置:/home/user/backups/    Tab:切换栏  q:退出│
└──────────────────────────────────────────────┘
```

---

## 十一、CLI（headless）

```
backup-tool backup [--app vscode] [--all] [--config path] [--format zip]
backup-tool restore <app> <file> [--target path] [--overwrite]
backup-tool history <app>
backup-tool apps list | add | remove
backup-tool check          # 校验配置
```

---

## 十二、里程碑

| 阶段 | 内容 | 交付 | 状态 |
|------|------|------|------|
| P0 | 核心引擎备份流程 + 配置加载 + 路径解析 + 基本 GUI（勾选→备份→进度） | 可日常使用 | 已完成 |
| P1 | 备份历史（index + 界面）+ 日志面板 + 设置页 + 排除规则 + CLI headless | 可调度备份 | 大部分完成（GUI 侧已完成，CLI headless 待收尾） |
| P2 | 恢复功能 + 内容预览 + 冲突策略 + retention 清理 | 完成闭环 | 部分完成：retention 与清理已交付，恢复向导为 UI 占位（完整恢复后续推进） |
| P3 | 内置定时器 + 系统通知 + TUI + 并行备份优化 | 完整版 | 待开始 |