[workspace]
resolver = "2"
members = ["crates/backup-core", "crates/backup-cli", "crates/gui"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[profile.release]
lto = true
strip = true