# AGENTS.md - 项目 AI 协作规范

## 项目指纹

- **技术栈**: 根据实际项目填写，如：Node.js / TypeScript / Python / Java
- **包管理器**: npm / pnpm / yarn / poetry / uv
- **测试框架**: Jest / Vitest / pytest / JUnit
- **代码检查**: ESLint / Black / Checkstyle

## 开发命令

- **安装依赖**: 根据项目填写
- **运行开发服务器**: 根据项目填写
- **类型检查**: 根据项目填写
- **运行 Lint**: 根据项目填写
- **运行测试**: 根据项目填写
- **构建**: 根据项目填写

## 代码风格与架构

- **目录结构**:
  - 源代码放在 `src/` 目录下
  - 测试文件与源文件同级或放在 `tests/` 目录
  - 配置文件放在项目根目录
- **编码规范**:
  - 使用严格类型模式，禁止使用 `any`（TypeScript）或避免动态类型（Python）
  - 单个函数/方法不超过 50 行
  - 单个文件不超过 300 行，超过需拆分
  - 所有公开 API/函数必须有文档注释
  - 使用有意义的变量和函数命名，避免缩写

## 安全红线与硬规则

- **只读目录**: 禁止修改 `node_modules/`、`dist/`、`build/`、`.next/` 等构建输出目录
- **敏感文件**: 禁止提交 `.env`、`.env.local`、`*.pem`、`*.key` 等包含密钥的文件
- **提交前检查**: 必须运行类型检查、Lint 和测试，全部通过才能提交
- **依赖管理**: 新增生产依赖前需要请求确认
- **数据库**: 禁止在生产环境直接修改数据库结构，必须通过迁移文件
- **日志**: 禁止在代码中硬编码敏感信息（密码、Token、密钥）

## 工作流规范

1. **规划阶段**: 任何新功能或重大变更，必须先由规划 Agent 输出任务分解计划
2. **开发阶段**: 开发者 Agent 按计划编码，每个任务完成后提交代码
3. **审查阶段**: 审查者 Agent 对代码进行独立审查，存在严重或主要问题则驳回
4. **迭代**: 审查不通过的代码，由开发者 Agent 修复后重新提交审查

## 任务跟踪

- 每个开发任务应有唯一编号（如 T-001, T-002）
- 提交信息格式: `[任务编号] 简短描述`
- 所有任务状态在计划文档中维护（待开始 / 进行中 / 已完成 / 已驳回）